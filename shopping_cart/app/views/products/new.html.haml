.panel
  %h1 New Product
  .form
    = form_for @product, method: 'POST' do |f|
      - if @product.errors.any?
        - @product.errors.full_messages.each do |err|
          #err-li= err
      %p
        .label='* Name'
        .medium-4=f.text_field :name
      %p
        .label= 'Description'
        .medium-4=f.text_area :description
      %p
        .label='* Price'
        .medium-4=f.text_field :price
      %p
        .label='Image'
        .medium-4=f.text_field :image_url

      %button.tiny.submit='Create'

  %p=link_to 'Back', products_path